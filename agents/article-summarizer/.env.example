# Default URLs for the article-summarizer agent.
# Copy this file to .env and replace the values with your own URLs.
# Add, remove, or reorder DEFAULT_URL_n entries as needed.
# Keys must follow the pattern DEFAULT_URL_<number> and be numbered consecutively.

DEFAULT_URL_1=https://example.com/
DEFAULT_URL_2=https://example.com/news/
